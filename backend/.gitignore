# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Dependencias instaladas com 'pip install --target libs' (ver manage.ps1).
# Alternativa ao venv em maquinas onde o Smart App Control bloqueia a copia
# do python.exe. Assim como o venv, e reconstruivel a partir do requirements.
libs/

# Ambiente (use .env.example como modelo)
.env

# Django
media/
staticfiles/
db.sqlite3

# ATENCAO: migrations/ NAO deve ser ignorado.
# As migracoes fazem parte do codigo e precisam ser versionadas.
